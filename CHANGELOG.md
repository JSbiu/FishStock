# 版本记录

FishStock 使用 `主版本.次版本.修订版本` 记录版本里程碑。功能里程碑滚动次版本号，优化、修复和发布准备仅滚动修订版本号。

## 0.7.4

- 新增当日盈亏：按行情归属交易日判定可用性，盘中、午休与收盘后照常显示，盘前、周末与节假日显示 `—`；刷新失败但数据仍属于当天时继续显示最后一次数值，避免刷新抖动导致数值在金额与占位符之间跳变。
- 当日盈亏按当前持仓数量估算，未计入当日交易与费用。Tree View 用「今日金额(百分比)」紧凑展示，百分比基准是昨收市值；浮盈百分比的基准是持仓成本——两者口径不同，分别回答「今天涨跌多少」和「我赚了多少」。
- Holdings Tree View 描述改为紧凑结构：个股是「股数 · 市值 · 浮盈(百分比) · 今日盈亏(百分比)」，币种分组是「市值 · 浮盈(百分比) · 今日盈亏(百分比)」。市值按万/亿压缩，盈亏保持完整金额，百分比只表示幅度、方向由金额的正负号表达，不再重复显示币种单位。
- Holdings View 新增视图排序与筛选，按收益率而不是当日涨跌幅排序；模式写入 `fishStock.viewOptions.v1` 的 `holdings` 键，旧数据回退为默认顺序，筛选后为空的币种分组直接隐藏。
- 股票与 ETF 自选为已在持仓的条目挂「持」角标，不改动文字颜色——TreeItem 的文字颜色只能整条目设置，染色会挤占表达涨跌方向的通道。匹配键为品类加代码，避免同一代码在股票自选与 ETF 自选之间误标，期货自选不参与。
- 编辑器管理器表格新增按币种的合计行，汇总市值、浮动盈亏、收益率与当日盈亏；未填全或没有行情的条目不计入，并在标签后标注待填写数量。
- `Quote` 增加 `tradingDate` 与 `quoteValidSince` 两个可选字段，只用于展示判定，不进持久化、不需要迁移；补充持仓排序筛选与当日盈亏判定的单元测试。

## 0.7.3

- 持仓管理器的添加卡片新增「从自选股添加」页签，与搜索并列；可从股票与 ETF 自选中批量勾选标的，一次加入持仓草稿后统一填写数量与平均成本。
- 自选候选按持仓边界过滤：仅保留 A 股、港股与境内 ETF，自动排除指数、期货与美股；已在持仓或草稿中的标的显示“已在表格中”并禁止重复添加。
- 自选条目按来源仓库推导品类并与行情缓存对应；过滤逻辑抽取为纯函数并增加单元测试，保存仍走既有整批校验，不改动持仓存储结构。

## 0.7.2

- 持仓管理器的每一行新增「调仓」，可记录一次买入或卖出，由扩展按移动加权平均成本重算数量与平均成本。
- 加仓按数量加权摊薄平均成本；减仓只改数量、平均成本不变，并提示本次已实现盈亏不计入。全部卖出的持仓在保存后移除。
- 调仓在行内展开方向、变动数量和成交价，实时预览新的数量与平均成本；数量或成交价非法、卖出超过持有数量时阻止应用。
- 调仓只修改内存草稿，沿用现有「保存全部」的整批校验与写入；不改动持仓存储结构，不需要数据迁移。
- 增加调仓计算单元测试，覆盖加仓摊薄、减仓成本不变、清仓、超量卖出与非法输入。

## 0.7.1

- 新增编辑器区域的可视化持仓管理器，可连续搜索、多选添加并在同一表格中修改多只持仓的数量与平均成本。
- 支持勾选多条持仓批量删除、放弃草稿和统一保存；任一行无效时阻止整批写入，不产生部分保存。
- Holdings 标题栏、添加命令和条目编辑入口统一进入管理器；Tree View 继续承担低干扰的持仓与收益概览。
- 管理器复用现有行情缓存、状态含义与涨跌颜色配置，不增加外部网络能力；关闭未保存页面时可选择保存、继续编辑或放弃。
- 增加批量草稿校验、未知标的拒绝和存储失败内存回滚测试，并把编辑器管理器纳入真实 Extension Host 冒烟。

## 0.7.0

- 新增独立 `Holdings` View，支持记录 A 股、港股和境内交易所 ETF 的持仓数量与平均成本；持仓默认空白，不创建虚构财务数据。
- 复用现有 Stock 与 Fund 行情缓存计算当前市值、浮动盈亏和收益率；休市或过期行情明确标注状态，无有效价格时不伪造收益。
- 人民币与港币持仓分别分组和汇总，不进行汇率换算；计算结果明确排除费用、税费、分红和公司行动影响。
- 状态栏继续保持单一项目，默认沿用行情轮播，用户可选择并持久保存持仓盈亏轮播模式。
- 持仓使用独立的版本化本地存储，不迁移或改写原有股票、基金和期货自选；脱敏诊断仅统计持仓数量与行情状态，不包含代码、数量、成本或盈亏。

## 0.6.1

- 将行情源时间与最近成功获取时间分开：价格或行情时间没有变化时，只要成功取得有效条目就不再误报数据过期。
- 交易中的单次刷新失败和单个标的临时缺失先保留有效 `live` 缓存并显示警告；持续失败超过配置阈值后才标记“刷新超时”。
- 以完整交易日而非当前日内小节判断行情是否有效，午休、下午开盘、收盘和期货夜盘跨自然日不再错误淘汰同一交易日行情。
- 细分“刷新超时”“等待当日行情”“行情时间异常”“时段未收录”四类过期原因，并纳入浮窗、Tree View 和脱敏诊断。

## 0.6.0

- 按 A 股、港股和国内商品期货的实际交易阶段控制自动刷新，覆盖午休、盘间休息、收盘、半日市、周末与节假日。
- 国内商品期货按品种区分夜盘至 23:00、次日 01:00、次日 02:30 或仅日盘，并正确处理周五夜盘、跨自然日和节假日前夜盘关闭。
- 统一 `live`、`closed`、`stale`、`error` 状态：休市保留最近行情，交易中超时或刷新失败显示数据过期，无有效行情才显示暂不可用。
- 自动刷新只请求当前交易中的条目，并在开盘、休息和收盘边界更新；手动刷新不受时段和自动退避限制，可在休市时获取最近行情。
- 期货搜索只允许添加交易时段已收录的商品品种；已有本地未知品种保持不变，并明确提示时段未收录。

## 0.5.6

- 移除启动阶段的 Tree View 预初始化，打开 VS Code 时不再切换到 FishStock 或错误停留在扩展侧边栏。
- 全新工作区首次进入 FishStock 时由 VS Code 默认展开 Stock、Fund 与 Futures；已有工作区继续使用用户保存的布局。
- 移除“展开一个 View 时联动展开另外两个”的首次初始化行为，各 View 始终独立折叠和展开。
- 不再依赖冷启动时不可靠的“上一侧边栏”历史恢复，尊重 VS Code 原本选择的活动侧边栏。

## 0.5.5

- 启动后预初始化 Stock、Fund 与 Futures 原生 Tree View，避免重载后首次展开时出现数秒空白。
- 三个 View 在重载后保持展开；如果启动时正在使用其他侧边栏，预初始化完成后自动切回。
- 行情刷新在 View 尚未显示时合并树更新，首次显示直接读取当前内存缓存；重复的分组折叠状态不再写入本地存储。

## 0.5.4

- 已有自选数据通过校验后不再在每次扩展激活时重复写回 `globalState`，减少 Tree View 出现前的本地存储等待。
- 增加慢存储回归测试，确保读取有效期货分组不会被无意义的持久化操作阻塞。
- Stock、Fund 或 Futures 从折叠状态变为可见时立即提交一次内存树刷新，不再等待下一次行情更新才显示分组。

## 0.5.3

- 扩展激活不再等待首次网络行情刷新完成；Stock、Fund 与 Futures 的本地分组先立即显示，行情随后在后台补齐。
- 首次后台刷新出现意外异常时继续写入 FishStock 输出日志，不阻塞视图和命令使用。

## 0.5.2

- 从 Marketplace Pre-Release 转入稳定更新通道，继续保留 Preview 产品成熟度标记。
- 状态栏支持按 Stock、Fund 与 Futures 的用户分组选择参与轮播的范围，默认保持全部分组参与。
- 轮播分组选择保存在当前 VS Code Profile；新建分组默认参与，分组重命名不会丢失选择。
- 全部取消时可关闭轮播，并可从状态栏或三个视图的菜单重新选择分组。
- 行情刷新或自选顺序变化后优先保持当前轮播条目，避免仅因列表位置变化突然跳项。

## 0.5.1

- 准备首个 VS Code Marketplace 公开预览版本，发布者统一为 `jsbiu`，并将扩展标记为 Preview 与 Pre-Release。
- 增加 MIT 许可证、Marketplace PNG 图标、商店元数据、隐私说明、支持说明和 GitHub 反馈模板。
- 重写面向用户的扩展介绍，明确本地数据、第三方网络请求、行情源访问限制和原生视图布局边界。
- 扩展发布门槛，自动校验发布者、许可证、预览标记、支持与隐私文档以及 VSIX 预发布属性。

## 0.5.0

- 新增独立 `Fund` View，支持境内交易所 ETF 的名称、拼音简称和代码搜索；“电网设备ETF”等同主题多只产品可分别预览选择。
- Fund 使用独立本地存储、分组、排序筛选、刷新、清空、恢复默认和全部展开命令，不与 Stock 自选混合。
- ETF 复用腾讯实时行情和统一状态栏轮播，并独立执行交易日守卫、缓存、过期判断、HTTP 403 退避与脱敏诊断。
- 首次创建基金自选时提供沪深300ETF示例；场外基金、ETF 联接基金、LOF 和港股 ETF 暂不纳入。

## 0.4.10

- 腾讯股票和新浪期货行情返回 HTTP 403 后，各自的自动刷新按 15 分钟、30 分钟、1 小时、2 小时、最长 6 小时递增退避，避免每分钟重复请求。
- 手动刷新不受退避限制，可在调整网络或代理后立即探测；成功后清零退避并恢复正常刷新。
- 脱敏诊断信息在退避期间显示下次自动探测时间。
- 股票与期货后台刷新分别判断自己的交易日，避免一个市场开市时连带请求当天休市的另一个模块。

## 0.4.9

- 打开 FishStock 时不再主动聚焦 Stock 或 Futures，避免刚打开的视图抢占另一视图的高度。
- 继续由 VS Code 保存用户调整后的双视图尺寸；双击分隔线可使用原生均分行为。

## 0.4.8

- 为 Stock 与 Futures 视图补充清单图标，消除新版 VS Code 的缺少 `icon` 属性警告。

## 0.4.7

- 移除可由 VS Code 根据视图和命令贡献自动生成的冗余激活声明，消除 `package.json` 编辑器警告。
- 保留启动完成激活，扩展原有启动行为不变。

## 0.4.6

- 增加主动复制的脱敏运行诊断信息，统计版本、配置、交易日、刷新和行情状态，不包含自选名称、代码或工作区信息。
- 增加独立 VS Code Extension Host 冒烟测试，覆盖扩展激活、视图聚焦、关键命令注册和全部展开。
- 建立统一发布门槛，自动检查 lint、编译、测试、版本一致性、VSIX 内容和打包结果。
- 增加维护者验收清单与随时反馈说明，用户和开发者不承担固定版本流程。

## 0.4.5

- 为 Stock 与 Futures 视图增加“全部展开”按钮，与 VS Code 原生“全部折叠”配对。
- 全部展开会同步保存分组状态，重新启动后仍保持展开。

## 0.4.4

- 修正 A 股 2024 年国庆节及港股 2025 年端午节、中秋节交易日数据。
- 修复“仅看上涨”和“仅看下跌”仍显示其他条目的问题。
- 补充交易日历与视图筛选回归测试，并明确不同市场的行情页来源。

## 0.4.3

- 将股票与期货两套命令注册合并为参数化命令集，行为与命令 ID 保持不变。
- 增加命令 ID 与扩展清单一致性测试。
- 全面核对 README、产品范围、架构和版本记录，使文档与 0.4 系列实现一致。

## 0.4.2

- Stock 与 Futures 视图各自支持展示方式：默认顺序、按涨跌幅升序或降序、仅看上涨或仅看下跌。
- 展示方式在视图标题栏切换并保存在本机，不影响自选数据的底层顺序。

## 0.4.1

- 点击自选条目在浏览器打开行情页：A 股、港股个股及 A 股指数使用东方财富，港股指数使用腾讯行情，国内期货使用新浪期货。

## 0.4.0

- 内置 A 股 2024–2026 年与港股 2025–2026 年交易所节假日休市数据，休市判断从时间区间猜测改为真实交易日历。
- 非交易日停止自动轮询行情；手动刷新、启动加载和配置变更后的刷新不受影响。
- 数据未覆盖的日期（2027 年及以后）回退为按工作日判断，待交易所公布后随版本更新。

## 0.3.9

- 全面核对 README、产品范围、架构和数据源说明，使文档与当前实现一致。
- 明确原生 View 的首次布局只在新工作区首次遇到 View 时生效，已有布局由 VS Code 恢复。
- 修正休市行情的短缓存计时依据，并增加缓存、过期和请求合并测试。
- 统一股票与期货共用仓库中的通用错误文案。

## 0.3.8

- 保留可独立显示或隐藏的 `Stock`、`Futures` 原生 View。
- 将新工作区首次布局声明为可见，并提供 `2:1` 初始高度权重。

## 0.3.7

- 增加沪金、白银、铜、沪铝和锡五个期货主连默认条目。
- 增加独立的期货清空和恢复默认命令。

## 0.3.6

- 增加国内商品期货主连和有效月份合约搜索、行情与独立 `Futures` View。
- 个股浮窗增加上市板块；期货浮窗展示昨结、成交量、持仓量和交易所。
- 移除 JSON 导入导出。

## 0.3.5

- 更新股票默认分组和条目；固定股票添加提示示例。

## 0.3.4

- 增加 A 股和港股市场指数搜索与行情，使用独立规范化后缀避免与个股代码冲突。

## 0.3.3

- 补充科创板搜索类型识别。
- 增加北交所官方新旧代码目录、按需缓存和完整代码直查回退。

## 0.3.2

- 将股票“清空全部数据”和“恢复默认数据”拆分为两个带确认的命令。

## 0.3.1

- Tree View 和单一状态栏复用同一行情浮窗。
- 状态栏统一显示名称、现价和涨跌幅。

## 0.3.0

- 个股浮窗增加日内价格、成交、换手率、PE(TTM) 和总市值。

## 0.2.0

- 增加中文名称、拼音简称和代码搜索预览。
- 支持从目标分组直接添加，以及分组、跨组移动和顺序管理。

## 0.1.0

- 完成 Activity Bar、原生 Tree View、本地自选、腾讯股票行情、刷新调度和单一状态栏原型。
